apiVersion: apps/v1
kind: Deployment
metadata:
  name: flaskapp-deployment
  labels:
    app: flaskapp
spec:
  replicas: 0 # Runs two copies of your app for high availability
  selector:
    matchLabels:
      app: flaskapp
  template:
    metadata:
      labels:
        app: flaskapp
    spec:
      containers:
      - name: flaskapp
        image: flaskapp:c0383f74a06e09b269f58ba302e2bffec5f7b4fa
        imagePullPolicy: IfNotPresent # Tells k3s to use the locally imported image if available
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: flaskapp-service
spec:
  selector:
    app: flaskapp
  ports:
    - protocol: TCP
      port: 8080        # The port the service listens on inside the cluster
      targetPort: 5000 # The port your Flask container is listening on
