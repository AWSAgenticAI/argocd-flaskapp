on:
  push:
    branches: [ "main" ]
jobs:
  build:
    # Target your specific runner directly and fix the lowercase tags
    runs-on: [self-hosted]

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: |
       ls
       # create docker image of python app
       docker build -t python-app1 .
       docker images
       
