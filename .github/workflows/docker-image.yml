name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: [self-hosted, backstage-runner, linux, x64]

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: |
       echo "hello world"
